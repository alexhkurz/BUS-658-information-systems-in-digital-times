---
description: BUS-658 bibliography — local paths; canonical workflow in bib-mcp
globs: bibliography/**/*.md, bibliography/**/README.md, references.bib
alwaysApply: false
---

# Bibliography (this repo)

**Canonical rules** (folder tree, PDF → text → Markdown, README line, note template, git):  
`../../../bib-mcp/docs/agent-rules/bibliography-workflow.md`  
(relative to this file: up from `.cursor/rules/` to `alexhkurz-at-git`, then `bib-mcp/...`).

## Overrides for BUS-658

- **BibTeX file:** `references.bib` (repository root).
- **Cited sources root:** `bibliography/` with thematic subfolders.
- **Note file relevance heading:** `## Relevance to our class` (map content to course topics).
- **Citation keys:** see `references.mdc` (author-year style, e.g. `commelin2023`).

Follow the canonical checklist there; only the paths and heading above differ.
